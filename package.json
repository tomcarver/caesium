{
  "name": "caesium",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app",
    "todo": "grep -ir --exclude-dir=node_modules --exclude=*~ \"TODO\" ."
  },
  "dependencies": {
    "express": "3.0.0"
  }
}
